// Single-language/currency for now (M1-M3 scope) — languages/currencies admin
// and a live selector land in a later milestone; these match the seed data.
export const DEFAULT_LOCALE = 'en';
export const DEFAULT_CURRENCY = 'USD';
export const CURRENCY_SYMBOL = '$';
